# Copy this file to `.env` and fill in real values.
# `.env` is git-ignored (never committed); this template is safe to commit.

# App identity
APP_NAME=AnalyticsOS API
APP_VERSION=0.1.0

# Port the API server listens on
PORT=3000
